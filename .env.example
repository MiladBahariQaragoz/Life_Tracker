# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (Prisma)
# For SQLite (default):
DATABASE_URL="file:./dev.db"
# For PostgreSQL:
# DATABASE_URL="postgresql://user:password@localhost:5432/mydb?schema=public"

# Frontend Configuration
# (Currently referenced in frontend config/build usage)
VITE_API_URL="http://localhost:3001"

# External Integrations
# Note: Google Calendar integration requires a 'service-account.json' file in the 'server' directory.
# Optional: Specify a calendar ID (e.g. your email) if sharing a specific calendar with the service account.
GOOGLE_CALENDAR_ID=your_email@gmail.com
